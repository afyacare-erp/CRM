<odoo>
    <record id="view_asset_register_report_form" model="ir.ui.view">
        <field name="name">report.account.asset.register.form</field>
        <field name="model">report.account.asset.register</field>
        <field name="arch" type="xml">
            <form string="Asset Register Report">
                <group>
                    <group>
                        <field name="date_to" required="1"/>
                    </group>
                    <group>
                        <field name="company_id" options="{'no_create': True}" invisible="1"/>
                    </group>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_asset_register_report" model="ir.actions.act_window">
        <field name="name">Asset Register Report</field>
        <field name="res_model">report.account.asset.register</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_asset_register_report"
        name="Asset Register (XLSX)"
        parent="account.menu_finance_reports"
        action="action_asset_register_report"
        sequence="100"/>

</odoo>